{
  "name": "generaltodo",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "deploy": "npm run build && gh-pages -d dist",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "gh-pages": "^6.0.0",
    "sass": "^1.68.0",
    "vite": "^4.4.5"
  }
}
